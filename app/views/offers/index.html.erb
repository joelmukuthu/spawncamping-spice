<h1>Offers</h1>

<%= render 'layouts/flash' %>

<%= render 'form' %>

<% if @offers.any? %>
  <div class="offers">
    <%= render partial: 'offer', collection: @offers %>
  </div>
<% else %>
  <div class="no_offers">
    No offers
  </div>
<% end %>
